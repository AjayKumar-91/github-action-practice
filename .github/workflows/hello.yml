name: Hello Ajay

# on:
#   push:
#     branches:
#       - master
#       - main

on:
  workflow_dispatch: 

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello users
        run: echo "Hello Dosto."

      - name: say hello username
        run: echo "Hello $USER."

  details:
    runs-on: ubuntu-latest
    steps:
      - name: version docker and python 
        run: docker --version && python --version
